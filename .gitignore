# secrets — NEVER commit
.env
.env.*
!.env.example
!.env.production.example

# python
__pycache__/
*.py[cod]
backend/venv/
venv/
.venv/
.pytest_cache/

# node / next
node_modules/
dashboard/node_modules/
dashboard/.next/

# generated
backend/static/contracts/
*.log
.DS_Store

# Local-only operational docs (public repo — keep strategy/ops private)
CLAUDE.md
GTM_OUTREACH.md

# Internal working memory (sprint log, project state) — not for the repo
memory/
