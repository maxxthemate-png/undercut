import { pageMeta } from '../_content/shared'

export const metadata = pageMeta(
  'Start your free 14-day trial — Undercut',
  'Create your Undercut account in 30 seconds: 14 days of full Starter features, no credit card. Connect eBay, set your floors, win the sale on autopilot.',
  '/signup'
)

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
