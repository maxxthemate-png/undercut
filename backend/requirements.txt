# Undercut Backend Dependencies
# Trimmed 2026-07-03: removed 20 packages with zero imports (ListingArb-era
# residue: supabase/redis/openai/langchain/crewai/playwright/aiohttp/requests/
# bs4/lxml/twilio/reportlab/pypdf/Pillow/tenacity/schedule/pytz/asyncpg/
# python-multipart). Dropping crewai+langchain also removes the Python<3.13 pin
# reason. psycopg2-binary stays: it's the DB driver via the URL scheme, not an import.

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.30.0

# Database
sqlalchemy==2.0.31
alembic==1.13.2
psycopg2-binary==2.9.9

# AI / LLM
anthropic==0.30.0

# HTTP client
httpx==0.27.0

# Notifications
sendgrid==6.11.0

# Data processing
pydantic==2.8.2
pydantic-settings==2.3.4

# Utilities
python-dotenv==1.0.1
structlog==24.4.0      # Structured logging
python-jose[cryptography]==3.4.0   # 3.4.0 fixes CVE-2024-33663 (algorithm confusion) + CVE-2024-33664 (JWE DoS)
passlib[bcrypt]==1.7.4
stripe>=11.0
